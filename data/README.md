# Local data only

This directory is intentionally empty in the public repository.

Source listings, scraped HTML, warehouse exports, real-data extracts, and generated synthetic datasets must remain local and are ignored by Git. Use only data you are authorised to collect and process.
